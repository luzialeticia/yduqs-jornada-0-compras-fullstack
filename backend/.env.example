# Conexao com o PostgreSQL (use o docker-compose da raiz do projeto)
DATABASE_URL="postgresql://yduqs:yduqs@localhost:5432/jornada_compras?schema=public"

# Porta da API
PORT=3000

# Origem permitida no CORS (frontend Vite)
FRONTEND_ORIGIN="http://localhost:5173"
